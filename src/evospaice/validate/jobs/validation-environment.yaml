# unify this environment with the root yaml python packages files

name: evospaice-validation
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - dendropy==5.0.13
      - scipy==1.17.1
